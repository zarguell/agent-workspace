[tool.pytest.ini_options]
asyncio_mode = "auto"
# The asyncpg pool holds connections bound to the loop that created them;
# a single session-wide loop keeps the control-plane engine usable across tests.
asyncio_default_fixture_loop_scope = "session"
asyncio_default_test_loop_scope = "session"
